package elecciones

import "time"

func reverseStringSlice(a []string) []string {
	for i := len(a)/2 - 1; i >= 0; i-- {
		opp := len(a) - 1 - i
		a[i], a[opp] = a[opp], a[i]
	}
	return a
}

func currentTime() time.Time {
	return time.Now()
}
